# PASTA-ML — pinned dependencies for reproducible benchmarks.
# Versions chosen to match the environment used to generate the paper's
# scalability tables and figures. Use:
#   pip install -r requirements.txt
streamlit==1.36.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
networkx==3.3
plotly==5.22.0
shap==0.46.0
joblib==1.4.2
# Optional but commonly used for image export of plotly figures
kaleido==0.2.1
