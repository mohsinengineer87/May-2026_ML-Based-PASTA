# PASTA-ML — pinned dependencies for reproducible benchmarks.
# Use:  pip install -r requirements.txt
# These versions all have pre-built wheels for Python 3.11
# (the version pinned in runtime.txt).
streamlit==1.36.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
networkx==3.3
plotly==5.22.0
shap==0.46.0
joblib==1.4.2
