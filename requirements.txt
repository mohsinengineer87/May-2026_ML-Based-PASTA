# PASTA-ML — dependencies for reproducible benchmarks.
# Use:  pip install -r requirements.txt
#
# Versions chosen so EVERY package has pre-built wheels for
# Python 3.11, 3.12, 3.13, AND 3.14 — so the build never falls
# back to compiling C extensions from source on Streamlit Cloud.
streamlit>=1.40.0,<2.0
pandas>=2.2.3,<3.0
numpy>=1.26.4,<3.0
scikit-learn>=1.5.2,<2.0
networkx>=3.3,<4.0
plotly>=5.24.0,<7.0
shap>=0.46.0,<1.0
joblib>=1.4.2,<2.0
python-docx>=1.1.0,<2.0
